name: synaps_generator
description: The generator used by synaps.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  source_gen: ^0.9.10+1
  build: '^1.6.0'
  synaps:
    path: ../

dev_dependencies:
  
