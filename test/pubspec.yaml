name: synaps_test
description: Test suite for Synaps.

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  synaps:
    path: ../
  synaps_generator:
    path: ../generator

dev_dependencies:
  test: ^1.14.4
  build_runner: ^1.10.11
